import { Typography } from '@mui/material';
import { FooterStyled } from './styles';

export const Footer = () => {
  return (
    <FooterStyled component="footer">
      <Typography>Servica</Typography>
      <Typography>@all rights are protected</Typography>
      <Typography>Created by Michael semkiv</Typography>
    </FooterStyled>
  );
};
